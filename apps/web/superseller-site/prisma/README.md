# Prisma (superseller-site)

NextAuth-style schema (Account, Session, User) with PostgreSQL. **PostgreSQL + Redis is now the primary database** for all app and customer data (migration from Firestore completed February 2026). Firestore fully removed (Feb 2026). `firebase-admin` kept for Storage only. See CLAUDE.md and .env.example for DATABASE_URL.
