# Prisma (rensto-site)

NextAuth-style schema (Account, Session, User) with PostgreSQL. **PostgreSQL + Redis is now the primary database** for all app and customer data (migration from Firestore completed February 2026). Firestore remains available as legacy/fallback only. See CLAUDE.md and .env.example for DATABASE_URL.
