# Prisma (rensto-site)

NextAuth-style schema (Account, Session, User) with SQLite. Used for optional local/auth database; **primary app and customer data is in Firestore**. See CLAUDE.md and .env.example for DATABASE_URL.
