# n8n (rensto-site)

**n8n/templates/** – JSON workflow templates used by the marketplace (download/customize flows). Synced from or to the main n8n instance (see infra/workflow-artifacts and CLAUDE.md).

**n8n/workflows/** – Reserved for workflow definitions; may be empty. Canonical workflow storage is the n8n instance and `infra/workflow-artifacts/`.
