---
phase: hair-approach-demo-package
task: 0
total_tasks: 5
status: planned
last_updated: 2026-03-12T00:30:00.000Z
---

<current_state>
Hair Approach Demo Package — ALL 5 PLANS WRITTEN AND VERIFIED. Ready to execute Wave 1.
</current_state>

<completed_work>
- CONTEXT.md written with all locked decisions
- 5 PLAN.md files created and verified by plan-checker (3 issues found, all fixed)
- All API signatures verified against actual codebase:
  - `generateImageKie()` in kie.ts — accepts KieImageRequest, polls internally, returns `{ url: string }`
  - `createKlingTask()` — returns taskId string, poll with `waitForTask(taskId, "kling")`
  - `uploadToR2(localPath, r2Key, contentType?)` — returns public URL string
  - `renderComposition()` in remotion-renderer.ts — accepts {compositionId, inputProps, outputPath, concurrency?, crf?, onProgress?}
  - fonts.ts exports FONT_FAMILY (Montserrat) and DISPLAY_FONT_FAMILY (Platypi)
  - Root.tsx has 14 existing Compositions (PropertyTour x4, CrewReveal x2, CrewDemo x2, CrewDemoV2 x2, CrewDemoV3 x2)
  - KenBurnsSlide.tsx, PhoneMockup.tsx, IntroCard.tsx, OutroCard.tsx all exist in components/
  - FPS=30, sec() helper converts seconds to frames
</completed_work>

<remaining_work>
## Wave 1 (parallel) — NEXT
- Plan 01: Create + run `apps/worker/src/scripts/hair-approach-upscale.ts` on RackNerd
  - Recraft Crisp upscale 6 photos via generateImageKie(), upload to R2 at hair-approach/portfolio/upscaled/
  - Cost: $0.015
- Plan 02: Create + run `apps/worker/src/scripts/hair-approach-motion.ts` on RackNerd
  - Kling 3.0 Std 3 motion clips via createKlingTask() + waitForTask()
  - Upload to R2 at hair-approach/motion/
  - Cost: $0.09

## Wave 2 (after Wave 1)
- Plan 03: Build HairShowreelComposition (Remotion) — new composition with Playfair Display, Ken Burns, 18s
  - Add font to fonts.ts, create props type, build component, register in Root.tsx, render script
  - Render on RackNerd, upload to R2 at hair-approach/showreel/
- Plan 04: Build SocialMockupComposition — 3 phone mockup stills
  - New composition with PhoneMockup + IG post UI, render 3 PNGs via renderStill()

## Wave 3 (after Wave 2)
- Plan 05: Integrate into /lp/hair-approach
  - Video hero with autoPlay/muted/loop/playsInline + poster fallback
  - Social mockup gallery section
  - Browser verification at 1440px (MANDATORY)
</remaining_work>

<decisions_made>
- Budget cap: $0.50 total, expected: $0.105
- Use Remotion (NOT manual FFmpeg) for all video composition
- Use original R2 photos for Kling input (not upscaled — rationale documented in Plan 02)
- generateImageKie() polls internally — no need for separate waitForTask call
- createKlingTask() ignores custom negative_prompt — uses KLING_PROPERTY_NEGATIVE auto
- All plans verified by plan-checker agent, 3 issues fixed
</decisions_made>

<context>
Plan files: .planning/phases/hair-approach-demo-package/*-PLAN.md
Key source files read and understood:
- apps/worker/src/services/kie.ts (generateImageKie, createKlingTask, waitForTask)
- apps/worker/src/services/r2.ts (uploadToR2)
- apps/worker/src/services/remotion-renderer.ts (renderComposition, ensureBundle)
- apps/worker/remotion/src/Root.tsx (14 existing compositions)
- apps/worker/remotion/src/config/fonts.ts (Montserrat + Platypi)
- apps/worker/remotion/src/config/timing.ts (FPS=30, sec() helper)
- apps/worker/remotion/src/components/ (KenBurnsSlide, PhoneMockup, IntroCard, etc.)
</context>

<next_action>
Execute Wave 1: Create both scripts (upscale + motion), deploy to RackNerd, run them.
Start with: /gsd:execute-phase hair-approach-demo-package
Or manually: write the two scripts, rsync to RackNerd, run, verify R2 uploads.
</next_action>
