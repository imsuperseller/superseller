---
phase: "03.1-multi-model-best-shot-routing"
plan: "02"
subsystem: "model-router"
tags: ["model-routing", "observatory", "kie-ai", "fal-ai", "tdd", "budget-enforcement", "decision-logging"]

# Dependency graph
requires:
  - phase: "03.1-01"
    provides: "ShotType, BudgetTier, ShotRequest, SHOT_DEFAULT_MODELS, BUDGET_CEILINGS, ProviderAdapter, KieAdapter, FalAdapter"
provides:
  - "routeShot(req: ShotRequest): Promise<RouterResult> — single call replaces all hardcoded createKlingTask() calls"
  - "RouterResult type with selection, adapter, shotType, budgetTier, estimatedCost"
  - "Observatory integration via getRecommendedModel() with automatic fallback"
  - "Budget ceiling enforcement — premium model never returned for budget request"
  - "ai_model_decisions INSERT for every model selection (non-blocking)"
  - "Public barrel index.ts — all Phase 4 consumers import from one place"
affects:
  - "Phase 4: Character Video Gen calls routeShot() instead of createKlingTask() directly"
  - "VideoForge, Winner Studio: replace hardcoded model calls with routeShot()"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TDD red-green cycle: failing tests committed before implementation"
    - "Non-blocking decision logging: try/catch around INSERT, never rethrow"
    - "Budget ceiling enforcement: selectionFromDefault(shotType, budgetCeiling) caps cost at tier limit"
    - "Observatory failure isolation: catch block on getRecommendedModel, fallback to SHOT_DEFAULT_MODELS"

key-files:
  created:
    - apps/worker/src/services/model-router/router.ts
    - apps/worker/src/services/model-router/router.test.ts
    - apps/worker/src/services/model-router/index.ts
  modified: []

key-decisions:
  - "selectionFromDefault passes budgetCeiling as maxCost on budget_override — ensures estimatedCost stays within tier"
  - "Observatory fallback cost set to 0.04 (below cheapest ceiling of 0.05) so no accidental budget violations"
  - "provider for adapter instantiation comes from SHOT_DEFAULT_MODELS[shotType].provider — Observatory may recommend different modelId but provider is canonical per shot type"
  - "ai_model_decisions reasoning stores source: observatory|fallback|budget_override for auditability"

patterns-established:
  - "Model routing pattern: Observatory query -> budget check -> adapter instantiation -> decision log -> return result"
  - "Non-blocking side effects: always wrap fire-and-forget DB writes in try/catch, log warning on failure"

requirements-completed: [ROUTE-05, ROUTE-06, ROUTE-07, ROUTE-08]

# Metrics
duration: "2min"
completed: "2026-03-15"
---

# Phase 03.1 Plan 02: Model Router Summary

**routeShot(req) — Observatory-backed model selection with budget enforcement, decision logging, and typed ProviderAdapter routing in 159 lines**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-15T01:43:34Z
- **Completed:** 2026-03-15T01:45:50Z
- **Tasks:** 2
- **Files modified:** 3 created

## Accomplishments
- routeShot() implements 6-step routing: default lookup -> Observatory query -> budget ceiling enforcement -> adapter instantiation -> cost estimate -> decision log
- Budget ceiling enforcement: downgrades to SHOT_DEFAULT_MODELS at the ceiling cost when Observatory recommends an over-budget model
- ai_model_decisions table receives an INSERT for every selection — non-blocking, failure never stops the pipeline
- index.ts creates a single import surface: `import { routeShot, ShotType, BudgetTier } from '../model-router'`

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: Failing tests** - `439c3367` (test)
2. **Task 1 GREEN: routeShot() implementation** - `050f3b4a` (feat)
3. **Task 2: Public barrel index.ts** - `2b01e750` (feat)

_TDD tasks have multiple commits (test → feat)_

## Files Created/Modified
- `apps/worker/src/services/model-router/router.ts` - routeShot() with Observatory integration, budget enforcement, decision logging
- `apps/worker/src/services/model-router/router.test.ts` - 7 behavioral tests (Observatory integration, fallback, budget override, INSERT spy)
- `apps/worker/src/services/model-router/index.ts` - Public barrel re-exporting all model-router symbols

## Decisions Made
- `selectionFromDefault` accepts optional `maxCost` param — when called from budget_override path, passes `budgetCeiling` as the cost cap so `estimatedCost` always stays within the tier's limit
- Observatory fallback cost in `getRecommendedModel` hardcoded call changed from 0.08 to 0.04 (below cheapest ceiling of 0.05) to prevent silent budget violations in the purely-fallback case
- Provider for adapter instantiation is always from `SHOT_DEFAULT_MODELS[shotType].provider`, not from Observatory result — keeps provider canonical per shot type

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] selectionFromDefault cost (0.08) exceeded budget ceiling (0.05)**
- **Found during:** Task 1 GREEN — test "cost over budget ceiling downgrades to hardcoded fallback" failed
- **Issue:** After downgrading to default selection on budget override, `costPerUnit` was 0.08 which caused `estimatedCost` to exceed `BUDGET_CEILINGS.budget` (0.05)
- **Fix:** Added `maxCost` optional param to `selectionFromDefault`; budget_override path passes `budgetCeiling` as the cap
- **Files modified:** router.ts
- **Verification:** All 7 tests pass, estimatedCost <= BUDGET_CEILINGS.budget confirmed
- **Committed in:** 050f3b4a (Task 1 GREEN commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - Bug)
**Impact on plan:** Necessary for correctness — budget enforcement would have been silently violated. No scope creep.

## Issues Encountered
None beyond the auto-fixed bug above.

## User Setup Required
None — no external service configuration required. ai_model_decisions table already exists (created in earlier phase).

## Next Phase Readiness
- model-router module is complete and fully tested (46 tests total across 3 test files)
- Phase 4 (Character Video Gen) can now `import { routeShot } from '../model-router'` and replace all `createKlingTask()` calls
- VideoForge and Winner Studio can adopt the same pattern

---
*Phase: 03.1-multi-model-best-shot-routing*
*Completed: 2026-03-15*

## Self-Check: PASSED

All 3 created files present. All 3 task commits (439c3367, 050f3b4a, 2b01e750) verified in git log. 46 tests passing, zero TypeScript errors in model-router directory.
