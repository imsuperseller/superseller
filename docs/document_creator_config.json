{
  "resource": "pdf",
  "operation": "create",
  "pdfTemplateId": "73077b23d5577066",
  "jsonParameters": false,
  "download": false,
  "propertiesUi": {
    "propertyValues": [
      {
        "key": "family-name",
        "value": "={{ $json.fullName || 'משפחה לא ידועה' }}"
      },
      {
        "key": "family-id",
        "value": "={{ $json.id || 'ID-' + Date.now() }}"
      },
      {
        "key": "total-income",
        "value": "={{ $json.income || '0' }} ש\"ח"
      },
      {
        "key": "family-size",
        "value": "={{ $json.familySize || '1' }}"
      },
      {
        "key": "city",
        "value": "={{ $json.city || 'לא צוין' }}"
      },
      {
        "key": "email",
        "value": "={{ $json.email || 'לא צוין' }}"
      },
      {
        "key": "phone",
        "value": "={{ $json.phone || 'לא צוין' }}"
      },
      {
        "key": "age",
        "value": "={{ $json.age || 'לא צוין' }}"
      },
      {
        "key": "profession",
        "value": "={{ $json.profession || 'לא צוין' }}"
      },
      {
        "key": "risk-score",
        "value": "={{ $json.riskScore || '0' }}/10"
      },
      {
        "key": "category",
        "value": "={{ $json.category || 'בינונית' }}"
      },
      {
        "key": "data-quality",
        "value": "={{ $json.dataQuality || '0' }}%"
      },
      {
        "key": "analysis",
        "value": "={{ $json.output.replace(/```html\\s*/g, '').replace(/```\\s*$/g, '').trim() }}"
      },
      {
        "key": "agency-name",
        "value": "שלי מזרחי - ביטוח | פנסיה | חיסכון"
      },
      {
        "key": "agency-id",
        "value": "SHELLY-001"
      }
    ]
  }
}
