# Launch
- [ ] Fill secrets via `wrangler secret put`
- [ ] Create KV namespaces & set IDs in `wrangler.toml`
- [ ] Seed at least one tenant in TENANT_REGISTRY_KV
- [ ] Create n8n workflow `/webhook/${N8N_WEBHOOK_PATH}` with verify Function
- [ ] Test `/health` then example curl
- [ ] Tag `staging-YYYYMMDD` then `prod-YYYYMMDD`